# Packages that should be collapsed into a single comparison
same_src "firefox-*", "firefox"
same_src "gcc-*"
same_src "gcj-*"
same_src "readline?"
same_src "glibc", "eglibc"
same_src "xulrunner-*"
same_src "telepathy-mission-control", "telepathy-mission-control-*"
same_src "xfree86-driver-synaptics", "xserver-xorg-input-synaptics"
same_src "ffmpeg-debian", "ffmpeg"
same_src "wxwindows*", "wxwidgets*"
same_src "mysql-dfsg-*", "mysql-5.*"
same_src "postgresql-*"
same_src "gnome-keyring", "libgnome-keyring"
same_src "db4.7", "db4.8", "db"
same_src "clutter-gtk", "clutter-gtk-*"
same_src "kdebluetooth", "kbluetooth"
same_src "wine", "wine1.2"
same_src "openoffice.org", "libreoffice"

# That's quite a build system you've got there buddy!
# Make the openoffice comparisons (somewhat sane).

#use_path "openoffice.org", "sources/*/"
#use_path "libreoffice", "sources/*/"

# Not really code
ignore_src "iso-codes"
ignore_src "language-support-*", "language-pack-*"
ignore_src "*l10n*"

# Duplicate copies of the kernel
ignore_src "linux-qcm-msm", "linux-ti-omap4" 
# Not really code
ignore_src "linux-firmware"

 
